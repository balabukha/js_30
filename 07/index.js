// cornify_add();

document.addEventListener('keydown', clicking);
let arr = [];
let magicWord = yahoo;

function clicking(e){
    arr.push(e.key);
    arr.splice(magicWord.length, )

    console.log(arr);


};